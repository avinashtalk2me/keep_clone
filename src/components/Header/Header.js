import React from "react";
import { connect } from "react-redux";
import "./Header.css";

const Header = () => {
  return (
    <nav>
      <p>Keep Clone</p>
    </nav>
  );
};

export default Header;